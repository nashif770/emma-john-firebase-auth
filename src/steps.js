/**
 * 1. crate a firebase project
 * 2. create a web application
 * 3. npm install firebase
 * 4. save firebase config
 * 5. export default app
 * 6. build > authentication > get started > enable sign in method
 * create sign up and login rout
 */

/**
 * 1. create a context provider (auth)
 * 2. create a context and set provider
 * 3. set children props
 * 4. set context value
 * 5. set the provider
 */

/**
 * hosting
 * ----------------
 * one time per pc
 * 1.  hosting 
 * 2. login
 * 
 * for each project 1 time
 * 1. firebase init
 * 2. proceed 
 * 3. hosting: firebase (up/down) use space bar to select
 * 4. use existing project
 * 5. select project with (up/down)
 * 6. which project as public directory: vite: dist; create-react-app: build;
 * 7. deploy single page? y
 * 8. continue deploy? n
 * 
 * 
 * for every time deploy
 * 
 * 1. npm run build
 * 2. firebase deploy
 * 
 */